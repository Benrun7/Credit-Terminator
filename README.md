# 💳 Credit Terminator

Динамический планировщик погашения кредитных карт (Dynamic Credit Card Payoff Planner)

## 📋 Описание

Веб-приложение для эффективного планирования и оптимизации погашения кредитных карт. Помогает пользователям:

- Строить динамический прогноз закрытия кредитных карт
- Оптимизировать график платежей
- Сравнивать различные стратегии погашения (Minimum, Snowball, Avalanche, Hybrid)
- Отслеживать эффективность текущей стратегии
- Рассчитывать потенциальную экономию при альтернативных сценариях

## 🏗️ Структура проекта

```
/project-root
  /client          # React + TypeScript + Vite фронтенд
  /server          # Node.js + Express + TypeScript бэкенд
  /shared          # Общие типы и константы
  .env             # Переменные окружения
  README.md
```

## 🚀 Быстрый старт

### Требования

- Node.js 18+ и npm
- Для MVP все данные хранятся в памяти сервера (перезапуск сервера удалит данные)

### Установка зависимостей

```bash
# Установить зависимости для всех проектов (из корневой директории)
npm install

# Или отдельно для клиента и сервера:
cd client && npm install
cd ../server && npm install
```

### Запуск в режиме разработки

```bash
# Из корневой директории - запустить клиент и сервер одновременно
npm run dev

# Или отдельно:
npm run dev:server  # Сервер на http://localhost:3001
npm run dev:client  # Клиент на http://localhost:3000
```

После запуска откройте браузер по адресу `http://localhost:3000`

### Сборка для production

```bash
npm run build
```

Затем запустите:
```bash
cd server && npm start
```

## 📦 Технологии

### Frontend
- React 18
- TypeScript
- Vite
- Recharts (графики)
- Axios (HTTP клиент)

### Backend
- Node.js
- Express
- TypeScript
- In-memory storage (для MVP)

## 🎯 Основные функции

### 1. Управление кредитными картами
- Добавление карт с параметрами (баланс, ставка, минимальный платёж, дата платежа)
- Редактирование параметров карт
- Удаление карт

### 2. Прогноз погашения
- Автоматический расчёт даты полного закрытия всех карт
- Месячная разбивка платежей
- Визуализация графика погашения долга
- Расчёт эффективности стратегии (КПД)

### 3. Сравнение стратегий
- **Minimum** - только минимальные платежи
- **Snowball** - погашение от меньшего баланса к большему
- **Avalanche** - погашение от большей ставки к меньшей
- **Hybrid** - комбинированная стратегия
- Визуальное сравнение результатов
- Рекомендации по оптимизации

## 📊 API Endpoints

### Карты
- `GET /api/cards` - получить все карты
- `GET /api/cards/:id` - получить карту по ID
- `POST /api/cards` - создать карту
- `PUT /api/cards/:id` - обновить карту
- `DELETE /api/cards/:id` - удалить карту

### Расчёты
- `POST /api/calculations/projection` - рассчитать прогноз
- `POST /api/calculations/compare` - сравнить стратегии

## 🔒 Безопасность

⚠️ **Disclaimer**: 
- Данные хранятся в памяти сервера (для MVP)
- Нет аутентификации пользователей
- Все расчёты носят информационный характер и не являются финансовой консультацией

## 📝 TODO для будущих версий

- [ ] Интеграция с базой данных (SQLite/PostgreSQL)
- [ ] Аутентификация пользователей
- [ ] Экспорт данных (CSV, PDF)
- [ ] Уведомления о предстоящих платежах
- [ ] История изменений
- [ ] Планирование будущих платежей
- [ ] Поддержка разных валют
- [ ] Grace period логика

## 📄 Лицензия

MIT

## 👨‍💻 Автор

Вениамин

---

**Версия**: 0.3.0

